{
  "name": "synccrm",
  "private": true,
  "description": "Front-end asset build for SyncCRM (only needed when changing resources/css/app.css)",
  "scripts": {
    "css": "tailwindcss -i resources/css/app.css -o public/assets/app.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
